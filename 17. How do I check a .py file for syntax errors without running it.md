### 🔎 Проверка `.py` файла на синтаксические ошибки без запуска

В Python есть несколько способов проверить код на корректность синтаксиса, не выполняя его:

---

## 📂 Способы проверки

1. **Через команду `python -m py_compile`**
```bash
python -m py_compile script.py
```
   👉 Если ошибок нет — создаётся скомпилированный файл (`.pyc`).  
   👉 Если есть ошибки — они будут выведены в консоль.

---

2. **Использовать `compileall`**
```bash
python -m compileall .
```
   👉 Проверяет все `.py` файлы в текущей директории.  
   👉 Удобно для больших проектов.

---

3. **Через IDE или редактор кода**
    - VS Code, PyCharm и другие редакторы подсвечивают синтаксические ошибки ещё до запуска.
    - Это работает благодаря встроенным линтерам.

---

4. **Линтеры и статический анализ**
    - `flake8`, `pylint`, `pyflakes` — инструменты, которые проверяют код на синтаксис и стиль.
```bash
flake8 script.py
pylint script.py
```

---

## 🧩 Пример

```bash
python -m py_compile my_code.py
```

👉 Если в коде ошибка, например:

```python
def func()
    print("Ошибка синтаксиса")
```

Ты получишь сообщение:

```
  File "my_code.py", line 1
    def func()
              ^
SyntaxError: invalid syntax
```

---

## 🎯 Итог

- **Быстрая проверка:** `python -m py_compile file.py`.
- **Массовая проверка:** `python -m compileall .`.
- **Глубокая проверка:** линтеры (`flake8`, `pylint`).

---
