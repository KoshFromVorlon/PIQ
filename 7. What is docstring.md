### 📖 Что такое docstring

**Docstring (documentation string)** — это специальная строка документации в Python, которая используется для описания
назначения модуля, класса, функции или метода.

---

## 🔑 Основные особенности

- **Формат:** пишется в тройных кавычках (`""" ... """` или `''' ... '''`) сразу после объявления объекта.
- **Назначение:** объясняет, что делает объект, какие у него параметры и что возвращается.
- **Доступ:** содержимое docstring можно получить через атрибут `.__doc__` или функцию `help()`.

---

## 🧩 Пример

```python
def add(a, b):
    """
    Функция складывает два числа.
    
    Параметры:
    a (int, float): первое число
    b (int, float): второе число
    
    Возвращает:
    int или float: сумма чисел
    """
    return a + b


print(add.__doc__)
```

👉 Вывод:

```
Функция складывает два числа.

Параметры:
a (int, float): первое число
b (int, float): второе число

Возвращает:
int или float: сумма чисел
```

---

## 🎯 Назначение docstring

- Делает код **самодокументируемым**.
- Упрощает работу в команде: каждый разработчик видит описание прямо в коде.
- Используется инструментами (`help()`, IDE, Sphinx) для генерации документации.

---

Таким образом, **docstring — это встроенный способ документировать код в Python**, который объединяет техническую
точность и эстетическую ясность.
