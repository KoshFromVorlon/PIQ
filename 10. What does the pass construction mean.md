### 🔎 Конструкция `pass` в Python

**`pass`** — это специальная инструкция, которая ничего не делает. Она используется как *заглушка* там, где синтаксис
требует наличие блока кода, но сам код пока отсутствует или не нужен.

---

## 📂 Основные случаи применения

1. **Пустые функции или методы**

```python
def todo_function():
    pass
```

👉 Функция определена, но пока не реализована.

---

2. **Пустые классы**

```python
class EmptyClass:
    pass
```

👉 Класс существует, но без атрибутов и методов.

---

3. **Заглушки в условных конструкциях**

```python
x = 10
if x > 0:
    pass  # пока ничего не делаем
else:
    print("x <= 0")
```

---

4. **Заглушки в циклах**

```python
for i in range(5):
    pass  # цикл выполняется, но действий нет
```

---

## 🎯 Итог

- `pass` — это **оператор пустого действия**.
- Он нужен для сохранения структуры программы, когда код ещё не написан или не требуется.
- Без `pass` в таких местах Python выдаст синтаксическую ошибку.

---
