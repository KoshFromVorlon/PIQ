# Генератор — это функция с ключевым словом yield. Он вычисляет элементы «лениво» (по одному), экономя память,
# в то время как список хранит всё содержимое в ОЗУ сразу.

def count_up_to(n):
    count = 1
    while count <= n:
        yield count
        count += 1
