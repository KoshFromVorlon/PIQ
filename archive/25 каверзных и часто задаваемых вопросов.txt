Вот список из 25 каверзных и часто задаваемых вопросов на собеседованиях на позицию backend Python-разработчика,
с развёрнутыми и технически точными ответами. Они охватывают темы Python, SQL, API, многопоточность, асинхронность,
безопасность и архитектуру.

🐍 Python — Глубокое понимание
Что происходит при импорте модуля в Python?
Ответ: Python сначала проверяет sys.modules, если модуль там — возвращает его. Если нет — ищет в sys.path, компилирует
в байткод (если .py есть), выполняет, и добавляет в sys.modules. Повторный импорт не приводит к повторному исполнению.

Объясните различие между is и ==.
Ответ: == сравнивает значения, а is — идентичность объектов в памяти. Например, a = [1]; b = [1]; a == b — True,
a is b — False.

Что такое GIL и как он влияет на многопоточность?
Ответ: GIL — Global Interpreter Lock, который блокирует одновременное исполнение байткода несколькими потоками
в CPython. Это делает threading неэффективным для CPU-bound задач, но приемлемым для I/O-bound.

Как работают декораторы?
Ответ: Декораторы — это функции, которые принимают другую функцию и возвращают модифицированную. Используются,
например, для логгирования, кэширования, авторизации.

Чем отличается @staticmethod от @classmethod?
Ответ: @staticmethod не получает ни self, ни cls, ведёт себя как обычная функция внутри класса. @classmethod
получает cls и может модифицировать состояние класса.

🗃 Базы данных и SQL
Что такое нормализация?
Ответ: Это процесс организации данных для устранения избыточности. Например, 3NF исключает транзитивные
зависимости: если A зависит от B, а B от C, то A не должен напрямую зависеть от C.

Что происходит при JOIN'е двух таблиц?
Ответ: БД создает декартово произведение строк с фильтрацией по ON-условию. Эффективность зависит от индексов и типа
соединения (INNER, LEFT, RIGHT, FULL).

Чем WHERE отличается от HAVING?
Ответ: WHERE фильтрует строки до GROUP BY, HAVING — после агрегирования. Например, HAVING COUNT(*) > 1 покажет
группы с более чем одним элементом.

Зачем использовать транзакции?
Ответ: Для обеспечения ACID-свойств: атомарность, согласованность, изолированность, долговечность. В Python — через
контекстный менеджер или вручную: conn.begin(), commit(), rollback().

Что такое индексы и когда они вредны?
Ответ: Индексы ускоряют чтение, но замедляют запись (insert/update/delete), увеличивают использование памяти и
требуют обновления. Неэффективны при низкой селективности.

🌐 API и веб
Что лучше: REST или GraphQL?
Ответ: REST — прост и кешируем, подходит для стандартных CRUD. GraphQL — позволяет клиенту запрашивать только нужные
поля, но сложнее в реализации и кешировании.

Что такое идемпотентность и где она важна?
Ответ: Идемпотентные методы (GET, PUT, DELETE) при многократном вызове не меняют результат. Важно при ретраях — чтобы
повторный запрос не привёл к дублированию данных.

Как реализовать пагинацию?
Ответ: Через LIMIT/OFFSET (менее эффективно) или keyset pagination — WHERE id > last_seen_id LIMIT n. Второй вариант
масштабируем и устойчив к изменениям данных.

Как защитить API от CSRF и XSS?
Ответ: CSRF — защита через токены, SameSite cookies. XSS — экранирование пользовательского ввода, CSP, валидация на
фронте и бэке.

Какие коды HTTP ошибок вы знаете?
Ответ:

400 — Bad Request

401 — Unauthorized

403 — Forbidden

404 — Not Found

409 — Conflict

429 — Too Many Requests

500 — Internal Server Error

⚙ Асинхронность и производительность
Когда использовать asyncio, а когда threading?
Ответ: asyncio лучше для I/O (например, API, файлы, сокеты), threading — для I/O, где библиотека не поддерживает async.
multiprocessing — для CPU-bound.

Как работает event loop в asyncio?
Ответ: Это цикл, который регистрирует задачи и запускает их по мере готовности (например, когда завершается
I/O операция). Он неблокирующий, переключается между задачами.

В чём опасность await внутри цикла?
Ответ: Если await вызывается в for-цикле, задачи выполняются последовательно. Чтобы параллелить — использовать
asyncio.gather().

Что быстрее: генераторы или списковые выражения?
Ответ: Генераторы — ленивые, потребляют меньше памяти. Списковые выражения — быстрее при малом объеме данных и
когда нужно сразу получить все значения.

Как профилировать Python-программу?
Ответ: Через модули cProfile, timeit, line_profiler, memory_profiler, tracemalloc. Важно профилировать именно
горячие участки (critical path).

🛡 Безопасность и архитектура
Как обезопасить хранение паролей?
Ответ: Только хэшировать, использовать bcrypt или argon2, с солью. Никогда не хранить пароли в открытом виде или
использовать MD5/SHA1.

Чем JWT может быть опасен?
Ответ: Если токен не истекает или не подписан, может быть украден. Также — использование none в alg, хранение в
localStorage (подвержено XSS).

Что такое race condition и как его избежать?
Ответ: Это ошибка из-за неконтролируемого параллельного доступа. Решения: блокировки (threading.Lock), атомарные
операции, транзакции, очереди.

Когда лучше использовать кеширование, и где?
Ответ: Для часто запрашиваемых неизменных данных. Используют Redis, Memcached, локальные functools.lru_cache.
Не забывать об инвалидировании и TTL.

Что выбрать для большого проекта: Django или FastAPI?
Ответ: Django — надёжный, с admin-панелью, ORM, удобен для стандартных проектов. FastAPI — современный, быстрый,
асинхронный, удобен для API-first приложений и микросервисов.