### 🔎 Разница между `list` и `tuple` в Python

Оба типа относятся к **последовательностям**, но у них есть принципиальные различия:

---

## 📂 Основные различия

| Характеристика         | `list`                                                      | `tuple`                                                |
|------------------------|-------------------------------------------------------------|--------------------------------------------------------|
| **Изменяемость**       | ✅ изменяемый (можно добавлять, удалять, изменять элементы)  | ❌ неизменяемый (элементы нельзя менять после создания) |
| **Синтаксис**          | `[1, 2, 3]`                                                 | `(1, 2, 3)`                                            |
| **Использование**      | Когда нужен динамический набор данных                       | Когда нужен фиксированный набор данных                 |
| **Производительность** | Медленнее, так как поддерживает операции изменения          | Быстрее при доступе к элементам                        |
| **Память**             | Требует больше памяти                                       | Более компактный                                       |
| **Хэшируемость**       | Не хэшируемый (нельзя использовать как ключ в `dict`)       | Хэшируемый, если все элементы внутри тоже хэшируемы    |
| **Методы**             | Имеет методы (`append`, `extend`, `remove`, `pop`, `sort`)  | Методов почти нет (только `count`, `index`)            |

---

## 🧩 Примеры

```python
# Список
numbers = [1, 2, 3]
numbers.append(4)  # [1, 2, 3, 4]
numbers[0] = 10  # [10, 2, 3, 4]

# Кортеж
coords = (10, 20, 30)
# coords[0] = 99  # ❌ Ошибка: кортеж неизменяемый
```

---

## 🎯 Итог

- **`list`** — гибкий, динамичный контейнер для изменяемых данных.
- **`tuple`** — компактный, быстрый и неизменяемый контейнер для фиксированных данных.

---
