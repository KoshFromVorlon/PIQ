### 🔎 Что такое `globals()` и `locals()` в Python

Обе функции относятся к **встроенным средствам introspection** (самоанализа), позволяющим получить доступ к
пространствам имён.

---

## 📂 `globals()`

- Возвращает **словарь глобальных переменных** текущего модуля.
- Включает:
    - имена функций,
    - классов,
    - импортированных модулей,
    - глобальные переменные.

```python
x = 10


def show_globals():
    print(globals())


show_globals()
# {'__name__': '__main__', 'x': 10, 'show_globals': <function ...>}
```

---

## 📂 `locals()`

- Возвращает **словарь локальных переменных** в текущей области видимости.
- Внутри функции покажет её локальные переменные.

```python
def show_locals():
    a = 5
    b = 7
    print(locals())


show_locals()
# {'a': 5, 'b': 7}
```

---

## 📂 Сравнение

| Характеристика        | `globals()`                    | `locals()`                                                           |
|-----------------------|--------------------------------|----------------------------------------------------------------------|
| **Область видимости** | Глобальная (всего модуля)      | Локальная (текущая функция/блок)                                     |
| **Тип данных**        | Словарь                        | Словарь                                                              |
| **Применение**        | Доступ к глобальным переменным | Доступ к локальным переменным                                        |
| **Изменяемость**      | Можно изменять значения        | Можно изменять, но не всегда влияет на реальные локальные переменные |

---

## 🎯 Итог

- **`globals()`** → словарь глобального пространства имён.
- **`locals()`** → словарь локального пространства имён.
- Вместе они позволяют динамически управлять переменными и анализировать окружение программы.

---
