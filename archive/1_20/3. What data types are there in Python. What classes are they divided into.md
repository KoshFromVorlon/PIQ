## 🔑 Базовые типы данных в Python

Python имеет встроенные типы данных, которые можно условно разделить на несколько классов:

### 1. **Числовые типы**

- `int` — целые числа (например, `42`, `-7`).
- `float` — числа с плавающей точкой (например, `3.14`, `-0.001`).
- `complex` — комплексные числа (например, `2+3j`).

### 2. **Логический тип**

- `bool` — логические значения: `True` или `False`.

### 3. **Последовательности**

- `str` — строки (например, `"Привет"`).
- `list` — списки (например, `[1, 2, 3]`).
- `tuple` — кортежи (например, `(1, 2, 3)`).
- `range` — диапазоны чисел (например, `range(0, 10)`).

### 4. **Множества**

- `set` — изменяемое множество (например, `{1, 2, 3}`).
- `frozenset` — неизменяемое множество.

### 5. **Отображения**

- `dict` — словари (например, `{"ключ": "значение"}`).

### 6. **Специальные типы**

- `NoneType` — объект `None`, обозначающий отсутствие значения.
- `bytes`, `bytearray`, `memoryview` — работа с двоичными данными.

---

## 📂 Классификация типов

Все типы данных в Python — это **классы**, а их значения — **объекты**.  
Можно выделить такие классы:

| Класс                      | Примеры                                              |
|----------------------------|------------------------------------------------------|
| **Простые (скалярные)**    | `int`, `float`, `complex`, `bool`, `NoneType`        |
| **Коллекции (контейнеры)** | `list`, `tuple`, `dict`, `set`, `frozenset`, `range` |
| **Текстовые и бинарные**   | `str`, `bytes`, `bytearray`, `memoryview`            |

---

## 🧩 Пример использования

```python
x = 42  # int
y = 3.14  # float
z = "Python"  # str
a = [1, 2, 3]  # list
b = {"ключ": "значение"}  # dict
c = {1, 2, 3}  # set
```

---

Таким образом, **все типы данных в Python — это классы**, а объекты, которые мы создаём, являются их экземплярами.

