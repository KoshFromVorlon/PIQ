### 🔎 Разница между многопоточными и многопроцессорными приложениями

Оба подхода связаны с параллельным выполнением задач, но принципиально различаются по уровню организации:
Для запоминания можно использовать аналогию из оптики:
**Процесс** - пучок белого света (белый), состоит из различных цветов - **Потоков** (красный, оранжевый, жёлтый, ...)
---

## 🧵 Многопоточность (multithreading)

- **Что это:** несколько потоков внутри одного процесса.
- **Общий адрес:** все потоки разделяют одну область памяти и ресурсы процесса.
- **Лёгкость:** создание потоков дешевле, чем процессов.
- **Коммуникация:** потоки легко обмениваются данными (так как память общая).
- **Минусы:**
    - возможны ошибки синхронизации (гонки данных, deadlock),
    - в Python есть **GIL (Global Interpreter Lock)**, который ограничивает одновременное выполнение байткода — поэтому
      многопоточность эффективна в задачах ввода‑вывода, но не всегда в вычислительных.

---

## ⚙️ Многопроцессность (multiprocessing)

- **Что это:** несколько независимых процессов, каждый со своей памятью и ресурсами.
- **Изоляция:** процессы не мешают друг другу, падение одного не рушит остальные.
- **Производительность:** может использовать все ядра процессора без ограничений GIL.
- **Коммуникация:** обмен данными сложнее (через очереди, пайпы, сокеты).
- **Минусы:**
    - создание процессов дороже, чем потоков,
    - больше расход памяти.

---

## 📂 Сравнение

| Характеристика        | Многопоточность               | Многопроцессность     |
|-----------------------|-------------------------------|-----------------------|
| **Уровень**           | Потоки внутри одного процесса | Отдельные процессы    |
| **Память**            | Общая                         | Раздельная            |
| **Стоимость запуска** | Дешевле                       | Дороже                |
| **Обмен данными**     | Легче                         | Сложнее               |
| **GIL в Python**      | Ограничивает                  | Не ограничивает       |
| **Лучше для**         | Задачи ввода‑вывода           | Вычислительные задачи |

---

## 🧩 Пример в Python

```python
# Многопоточность
import threading


def worker():
    print("Поток работает")


t = threading.Thread(target=worker)
t.start()

# Многопроцессность
from archive import multiprocessing_demo


def worker():
    print("Процесс работает")


p = multiprocessing.Process(target=worker)
p.start()
```

---

## 🎯 Итог

- **Многопоточность** → лёгкая параллельность внутри одного процесса, удобна для ввода‑вывода.
- **Многопроцессность** → независимые процессы, эффективна для вычислений на нескольких ядрах.

---
