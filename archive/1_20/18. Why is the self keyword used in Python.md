### 🔎 Зачем используется `self` в Python

**`self`** — это соглашение в Python, обозначающее **ссылку на текущий экземпляр класса**. Оно используется внутри
методов класса, чтобы обращаться к атрибутам и другим методам именно этого объекта.

---

## 📂 Основные моменты

- **Первый аргумент метода**  
  Внутри класса любой метод, кроме статических, принимает первым параметром ссылку на объект. По соглашению этот
  параметр называется `self`.
  ```python
  class MyClass:
      def __init__(self, value):
          self.value = value  # сохраняем значение в объекте

      def show(self):
          print(self.value)   # обращаемся к атрибуту через self
  ```

- **Доступ к атрибутам и методам**  
  Через `self` можно:
    - хранить данные (`self.x = 10`),
    - вызывать другие методы того же объекта (`self.method()`).

- **Не ключевое слово языка**  
  Важно: `self` не является зарезервированным словом Python. Это просто соглашение. Можно назвать его иначе, но это
  нарушит читаемость и стиль (PEP 8 рекомендует использовать именно `self`).

---

## 🧩 Пример

```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def move(self, dx, dy):
        self.x += dx
        self.y += dy


p = Point(2, 3)
p.move(1, -1)
print(p.x, p.y)  # 3 2
```

👉 Здесь `self` гарантирует, что изменения происходят именно в объекте `p`.

---

## 🎯 Итог

- **`self`** — ссылка на текущий объект.
- Нужен для доступа к атрибутам и методам экземпляра.
- Это соглашение, а не ключевое слово, но его использование — стандарт де‑факто.

---
