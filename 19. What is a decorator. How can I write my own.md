### 🔎 Что такое декоратор в Python

**Декоратор** — это функция, которая принимает другую функцию или метод как аргумент и возвращает новую функцию с
дополнительным поведением.  
Он позволяет **расширять функциональность** без изменения исходного кода.

---

## 📂 Как работают декораторы

- В Python функции — объекты первого класса, их можно передавать и возвращать.
- Декоратор оборачивает функцию в другую функцию («wrapper»), добавляя логику до или после её вызова.
- Записывается через синтаксис `@decorator_name` над определением функции.

---

## 🧩 Пример простого декоратора

```python
def my_decorator(func):
    def wrapper(*args, **kwargs):
        print("Перед вызовом функции")
        result = func(*args, **kwargs)
        print("После вызова функции")
        return result

    return wrapper


@my_decorator
def say_hello(name):
    print(f"Привет, {name}!")


say_hello("Миша")
```

👉 Вывод:

```
Перед вызовом функции
Привет, Миша!
После вызова функции
```

---

## 📂 Практическое применение

- **Логирование** — отслеживание вызовов функций.
- **Проверка прав доступа** — выполнение функции только при определённых условиях.
- **Кэширование** — сохранение результатов для ускорения повторных вызовов.
- **Валидация данных** — проверка аргументов перед выполнением.

---

## 🧩 Пример с параметрами

```python
def repeat(n):
    def decorator(func):
        def wrapper(*args, **kwargs):
            for _ in range(n):
                func(*args, **kwargs)

        return wrapper

    return decorator


@repeat(3)
def greet(name):
    print(f"Привет, {name}!")


greet("Миша")
```

👉 Функция `greet` выполнится 3 раза подряд.

---

## 🎯 Итог

- **Декоратор** — это способ элегантно расширить функциональность функций и методов.
- Он сохраняет симметрию и чистоту кода: исходная функция остаётся неизменной, а дополнительная логика добавляется
  «снаружи».

---
