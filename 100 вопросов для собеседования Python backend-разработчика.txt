🐍 Python (язык, ООП, основы)
Что такое GIL и как он влияет на многопоточность?
Ответ: GIL блокирует выполнение байткода одним потоком, делая многопоточность неэффективной для CPU-bound задач.

Чем отличается is от ==?
Ответ: is сравнивает идентичность объектов, == — их значения.

Что делает @staticmethod?
Ответ: Создаёт метод, не получающий self.

Что такое декоратор?
Ответ: Функция, модифицирующая поведение другой функции.

Что такое list comprehension?
Ответ: Краткий способ создания списков.

Что такое генератор?
Ответ: Объект, создающий значения по мере запроса (через yield).

Как работает with?
Ответ: Управляет ресурсами (файлы, подключения), автоматически закрывает их.

Что делает *args, **kwargs?
Ответ: Принимают переменное число позиционных и именованных аргументов.

Что такое duck typing?
Ответ: Тип объекта определяется по поведению, а не по наследованию.

Чем отличается deepcopy от copy?
Ответ: copy копирует верхний уровень, deepcopy — всё рекурсивно.

🧰 FastAPI, Django, Flask
Чем FastAPI лучше Flask?
Ответ: Поддержка async, OpenAPI, валидация через Pydantic.

Как работает зависимость в FastAPI?
Ответ: Через Depends, инжектится как аргумент функции.

Что делает @app.middleware?
Ответ: Оборачивает весь запрос/ответ, используется для логгирования, трекинга и т.д.

Как в Django устроены миграции?
Ответ: Генерируются через makemigrations, применяются через migrate.

Что такое urlpatterns в Django?
Ответ: Список маршрутов (url patterns).

Чем отличается Form от Body в FastAPI?
Ответ: Form парсит данные формы, Body — JSON.

Что такое CBV в Django?
Ответ: Class-Based View — представления на основе классов.

Как работает сериализация в Django REST Framework?
Ответ: Сериализаторы конвертируют модели в JSON и обратно.

Что такое middleware?
Ответ: Код, который выполняется до и после обработки запроса.

Как валидировать входные данные в FastAPI?
Ответ: Через Pydantic-модели.

🗃️ Базы данных и SQL
Что такое ORM?
Ответ: Объектно-реляционное отображение (работа с БД через классы).

Чем отличается LEFT JOIN от INNER JOIN?
Ответ: LEFT возвращает все из левой таблицы, даже если нет совпадений.

Что такое нормализация?
Ответ: Устранение избыточности и дублирования данных.

Что делает индекс?
Ответ: Ускоряет выборку, замедляет вставку/обновление.

Что такое транзакция?
Ответ: Группа операций, выполняемых атомарно.

Зачем нужны миграции?
Ответ: Для безопасного изменения схемы БД.

Что такое ACID?
Ответ: Принципы надёжности БД: атомарность, согласованность, изоляция, долговечность.

Как сделать пагинацию на SQL?
Ответ: С помощью LIMIT и OFFSET.

Какой тип индекса лучше для поиска по диапазону?
Ответ: B-tree.

Чем отличается SQL от NoSQL?
Ответ: SQL — табличная структура, NoSQL — документы, графы, колонки и т.д.

⚙️ Асинхронность и очереди
Как работает async/await?
Ответ: Позволяет не блокировать поток при I/O-операциях.

Что такое event loop?
Ответ: Цикл, обрабатывающий async-задачи.

Что делает asyncio.gather()?
Ответ: Запускает несколько корутин параллельно.

Что такое Celery?
Ответ: Система фоновых задач с брокером (Redis, RabbitMQ).

Что такое брокер в Celery?
Ответ: Очередь сообщений, где хранятся задачи.

Как обрабатывать ошибки в async функции?
Ответ: Так же, как и в sync — через try/except.

Чем отличается thread от coroutine?
Ответ: Thread — OS-поток, coroutine — легковесная структура в event loop.

Когда нужна асинхронность?
Ответ: При высоком количестве I/O: запросы, файлы, API.

Что такое rate limiting?
Ответ: Ограничение количества запросов за время.

Как отменить async-задачу?
Ответ: Через .cancel() и проверку CancelledError.

🧪 Тестирование и качество
Что такое pytest?
Ответ: Фреймворк для написания и запуска тестов.

Что такое фикстура?
Ответ: Подготовка данных/среды для теста.

Чем отличаются юнит-тесты и интеграционные?
Ответ: Юнит — изолированно, интеграционные — в связке с другими компонентами.

Что делает mock?
Ответ: Подменяет зависимости на заглушки.

Как проверить покрытие кода?
Ответ: С помощью coverage.py.

Что делает tox?
Ответ: Запускает тесты в разных окружениях Python.

Что такое pre-commit?
Ответ: Хуки, запускающиеся перед коммитом (например, black, flake8).

Чем assert отличается от pytest.raises?
Ответ: assert проверяет условие, raises — ожидает исключение.

Что делает isort?
Ответ: Сортирует импорты по группам.

Что такое contract test?
Ответ: Проверка интерфейсов между сервисами (например, API).

☁️ DevOps и окружение
Что такое Docker и зачем он нужен?
Ответ: Изолирует среду разработки и деплоя.

Что делает docker-compose?
Ответ: Запускает набор контейнеров с зависимостями.

Что такое CI/CD?
Ответ: Автоматизация тестов, сборки и выката.

Что такое .env файл?
Ответ: Хранит переменные окружения.

Чем .dockerignore отличается от .gitignore?
Ответ: Указывает, что не копировать в Docker image.

Как настроить GitHub Actions?
Ответ: Через .github/workflows/*.yml

Что такое Helm?
Ответ: Менеджер пакетов для Kubernetes.

Как обновить зависимости в проекте?
Ответ: pip install -U, poetry update, или pip-tools.

Что такое Linter?
Ответ: Проверка кода на соответствие стилю и ошибкам.

Что делает Makefile?
Ответ: Автоматизирует команды (make test, make run и т.д.)

🧱 Архитектура и дизайн
Что такое SOLID-принципы?
Ответ: Набор принципов проектирования: SRP, OCP, LSP, ISP, DIP.

Когда использовать сервисный слой?
Ответ: Когда бизнес-логика не должна находиться в контроллерах или репозиториях.

Что такое idempotency в API?
Ответ: Повторный вызов даёт тот же результат, что и первый.

Зачем нужны репозитории?
Ответ: Отделяют доступ к данным от бизнес-логики.

Что такое DTO и зачем он нужен?
Ответ: Data Transfer Object — для передачи только нужных данных между слоями.

Что такое CQRS?
Ответ: Разделение моделей чтения и записи.

Что такое event-driven архитектура?
Ответ: Архитектура, построенная вокруг событий и их обработчиков.

Когда использовать monolith, а когда microservices?
Ответ: Monolith — проще для старта, microservices — при масштабировании команд и нагрузки.

Чем отличается REST от RPC?
Ответ: REST ориентирован на ресурсы, RPC — на действия.

Что такое middleware и где он используется?
Ответ: Прослойка между запросом и обработкой (например, логгирование, трейсинг, аутентификация).

🌐 API и интеграции
Что такое RESTful API?
Ответ: API, использующее стандарты REST: методы HTTP, URL как ресурсы и т.д.

Что такое OpenAPI/Swagger?
Ответ: Стандарты для описания API, генерации документации и клиентов.

Какой HTTP-метод используется для обновления ресурса?
Ответ: PUT (полное обновление), PATCH (частичное).

Когда вернуть 400, а когда 422?
Ответ: 400 — некорректный запрос в целом, 422 — валидация прошла, но данные неверны.

Чем отличается 401 от 403?
Ответ: 401 — неавторизован, 403 — нет доступа.

Как ограничить количество запросов к API?
Ответ: Через rate limiter — в middleware или API gateway.

Что такое HATEOAS?
Ответ: Гипермедиа как движок состояния — REST-ответы содержат ссылки на возможные действия.

Что такое API gateway?
Ответ: Прокси, маршрутизирующий и защищающий запросы к микросервисам.

Как тестировать внешний API?
Ответ: Использовать mock-сервер или контрактные тесты.

Что такое webhook?
Ответ: Механизм уведомлений от внешнего сервиса по URL.

🧠 Типизация и документация
Что такое type hints?
Ответ: Аннотации типов в Python (def foo(x: int) -> str:).

Зачем использовать mypy?
Ответ: Для статической проверки типов.

Что делает TypedDict?
Ответ: Описывает словарь с конкретной структурой.

Что такое Optional[str]?
Ответ: Значение может быть str или None.

Что такое Union и когда он полезен?
Ответ: Объединение типов (Union[str, int]) — полезно при разных форматах данных.

Зачем нужны Pydantic-модели?
Ответ: Для строгой валидации и сериализации данных.

Чем dataclass отличается от Pydantic?
Ответ: dataclass не валидирует, но быстрее и проще; Pydantic — строгий контроль.

Как задокументировать Python-проект?
Ответ: Docstrings, Markdown-README, Sphinx/Docs-as-code.

Что такое stub-файлы (.pyi)?
Ответ: Файлы для аннотации типов внешних библиотек.

Что делает Protocol в typing?
Ответ: Определяет интерфейс без наследования — аналог duck typing с проверкой.

🔐 Безопасность и best practices
Как защитить пароли в базе?
Ответ: Хэшировать (bcrypt, argon2), не хранить в открытом виде.

Что такое SQL-инъекция?
Ответ: Вставка вредоносного SQL через пользовательский ввод.

Как защитить API от CSRF?
Ответ: Через токены, SameSite cookies, заголовки.

Что такое XSS?
Ответ: Вставка JS-кода в данные, которые потом рендерятся на клиенте.

Что делает secrets модуль в Python?
Ответ: Генерация криптографически безопасных токенов.

Как хранить секреты в приложении?
Ответ: Через .env, Vault, Kubernetes secrets, CI variables.

Зачем использовать HTTPS?
Ответ: Шифрует данные между клиентом и сервером.

Что такое rate limiting?
Ответ: Ограничение частоты запросов от одного клиента.

Что такое input sanitization?
Ответ: Очистка и проверка пользовательского ввода от потенциальных угроз.

Как проверить уязвимости в зависимостях?
Ответ: Использовать pip-audit, safety, dependabot.