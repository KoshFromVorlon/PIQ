### 🔎 Как просмотреть методы объекта в Python

В Python есть несколько способов узнать, какие методы и атрибуты доступны у объекта:

---

## 📂 Основные инструменты

1. **`dir()`**  
Возвращает список всех атрибутов и методов объекта.
```python
nums = [1, 2, 3]
print(dir(nums))
```
👉 Выведет список методов списка (`append`, `extend`, `pop`, и т.д.).

---

2. **`help()`**  
Показывает встроенную документацию по объекту и его методам.
```python
nums = [1, 2, 3]
help(list)
help(nums.append)
```
👉 Можно получить описание конкретного метода.

---

3. **Атрибут `.__doc__`**  
Позволяет прочитать строку документации (docstring) объекта или метода.
```python
nums = [1, 2, 3]
print(list.__doc__)
print(nums.append.__doc__)
```

---

4. **Модуль `inspect`**  
Даёт расширенные возможности анализа: список членов, сигнатуры функций, docstring.
```python
import inspect
nums = [1, 2, 3]
print(inspect.getmembers(nums, predicate=inspect.ismethod))
```
👉 Покажет все методы объекта `nums`.

---

## 🧩 Пример

```python
class MyClass:
    def method1(self):
        pass

    def method2(self):
        pass


obj = MyClass()

print(dir(obj))  # список атрибутов и методов
help(obj)  # документация
```

---

## 🎯 Итог

- **`dir()`** → быстрый список методов.
- **`help()`** → подробная справка.
- **`.__doc__`** → краткая документация.
- **`inspect`** → расширенный анализ.

---