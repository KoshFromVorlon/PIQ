**PEP 8 — это официальный стиль‑гайд для написания кода на Python, созданный для того, чтобы код был единообразным,
читаемым и понятным.**

---

## 📜 Основные сведения

- **Полное название:** *PEP 8 – Style Guide for Python Code*.
- **Авторы:** Гвидо ван Россум, Барри Варсо, Ник Коглан и другие.
- **Дата создания:** 5 июля 2001 года.
- **Статус:** активный документ, который продолжает обновляться и адаптироваться под изменения языка.
- **Цель:** задать единые правила оформления кода, чтобы разные разработчики могли легко читать и поддерживать чужие
  программы.

---

## 🔑 Основные правила PEP8

- **Имена переменных и функций:** использовать `snake_case` (например, `my_function`).
- **Имена классов:** использовать `CamelCase` (например, `MyClass`).
- **Отступы:** 4 пробела, без табуляции.
- **Максимальная длина строки:** 79 символов.
- **Пробелы:**
    - вокруг операторов (`a = b + c`),
    - но не внутри скобок (`func(a, b)`).
- **Комментарии:**
    - блоки комментариев начинаются с `#`,
    - docstring оформляются в тройных кавычках `"""`.
- **Импорты:** каждый модуль импортируется отдельно, стандартные библиотеки идут первыми.

---

## 🎯 Назначение

- **Читаемость:** код становится понятным даже для тех, кто видит его впервые.
- **Единообразие:** проекты разных команд выглядят одинаково.
- **Поддерживаемость:** легче исправлять ошибки и развивать проект.
- **Инструменты:** существуют линтеры (например, `flake8`, `pylint`) и автоформаттеры (`black`, `autopep8`), которые
  помогают автоматически проверять и исправлять стиль кода.

---

## 🧩 Пример

```python
# Правильно по PEP 8
def calculate_area(width, height):
    return width * height


# Неправильно
def CalculateArea(width, height): return width * height
```

---

👉 Итак, **PEP 8 — это не про логику программы, а про её внешний вид и удобство чтения**.  
