### 🔎 Что может быть ключом в словаре Python

В Python словарь (`dict`) — это структура данных вида **ключ → значение**.  
Ключи должны удовлетворять определённым условиям:

---

## 📂 Требования к ключам

1. **Хэшируемость**
    - Ключ должен быть объектом, который имеет фиксированное значение хэша (`__hash__`).
    - Это нужно для быстрого поиска в словаре.

2. **Неизменяемость**
    - Ключи должны быть неизменяемыми, чтобы их хэш не менялся во время работы программы.
    - Примеры допустимых типов:
        - числа (`int`, `float`),
        - строки (`str`),
        - кортежи (`tuple`), если все элементы внутри тоже неизменяемы.

3. **Уникальность**
    - В словаре не может быть двух одинаковых ключей.
    - Если присвоить значение по существующему ключу, оно перезапишет старое.

---

## 🧩 Примеры

```python
# Допустимые ключи
my_dict = {
    1: "число",
    "name": "строка",
    (10, 20): "кортеж"
}

# Недопустимые ключи
bad_dict = {
    [1, 2, 3]: "список",  # ❌ Ошибка: список изменяемый
    {1, 2}: "множество"  # ❌ Ошибка: множество изменяемое
}
```

---

## 🎯 Итог

- ✅ Ключами могут быть **неизменяемые и хэшируемые объекты** (числа, строки, кортежи из неизменяемых элементов).
- ❌ Нельзя использовать изменяемые объекты (списки, множества, другие словари).

---
