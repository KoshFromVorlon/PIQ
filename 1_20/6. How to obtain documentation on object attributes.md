## 📚 Способы получить документацию по атрибутам объекта

### 1. **Функция `help()`**

```python
help(str)
```

- Выведет встроенную документацию для класса `str` и его методов.
- Можно вызвать для конкретного метода:

```python
help(str.upper)
```

### 2. **Функция `dir()`**

```python
dir(list)
```

- Покажет список всех атрибутов и методов объекта.
- Это быстрый способ узнать, что доступно у объекта.

### 3. **Атрибут `.__doc__`**

```python
print(str.__doc__)
print(str.upper.__doc__)
```

- Возвращает строку документации (docstring), если она определена.

### 4. **Модуль `inspect`**

```python
import inspect

print(inspect.getdoc(str))
print(inspect.getmembers(str))
```

- Позволяет получить docstring, список методов, сигнатуры функций и даже исходный код.

---

## 🧩 Пример

```python
class MyClass:
    """Это пример класса."""

    def method(self):
        """Метод делает что-то."""
        return "Hello"


obj = MyClass()

print(obj.__doc__)  # "Это пример класса."
print(obj.method.__doc__)  # "Метод делает что-то."
help(obj)  # Полная справка
```

---

## 🎯 Итог

- **`help()`** → интерактивная справка.
- **`dir()`** → список атрибутов.
- **`.__doc__`** → строка документации.
- **`inspect`** → расширенный анализ.

---
