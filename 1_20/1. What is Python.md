**Python — это высокоуровневый, мультипарадигменный язык программирования общего назначения, созданный для удобства,
читаемости и быстрой разработки.**

---

### 🔑 Основные факты о Python

- **Автор:** Гвидо ван Россум, впервые представлен 20 февраля 1991 года.
- **Тип языка:** интерпретируемый, с динамической строгой типизацией и автоматическим управлением памятью.
- **Парадигмы:** поддерживает процедурное, объектно-ориентированное, функциональное и асинхронное программирование.
- **Кроссплатформенность:** работает на Windows, Linux, macOS и многих других системах.
- **Актуальная версия:** Python 3.14.1 (выпущена 2 декабря 2025 года).
- **Лицензия:** Python Software Foundation License.

---

### 🌍 Где используется Python

Python стал одним из самых популярных языков благодаря своей простоте и универсальности:

- **Веб-разработка** (Django, Flask).
- **Анализ данных и машинное обучение** (NumPy, Pandas, TensorFlow, PyTorch).
- **Автоматизация и скрипты** (упрощение рутинных задач).
- **Кибербезопасность и финтех** (инструменты анализа и моделирования).
- **Игровая разработка** (например, Pygame).
- **Научные исследования и искусственный интеллект**.

---

### 📌 Почему Python так популярен

- **Простой синтаксис:** близок к естественному языку, легко читается и пишется.
- **Большое сообщество:** тысячи библиотек и активная поддержка разработчиков.
- **Гибкость:** одинаково хорошо подходит для прототипирования и создания крупных систем.
- **Образование:** часто используется как первый язык для обучения программированию благодаря своей доступности.

---

### 🧩 Пример кода на Python
Этот код демонстрирует простоту синтаксиса и читаемость Python.

```python
# Вывод чисел Фибоначчи до 1000
def fib(n):
    a, b = 0, 1
    while a < n:
        print(a, end=' ')
        a, b = b, a + b
    print()


fib(1000)
```

Примеры кода, который решает одну и ту же задачу, но использует четыре разные парадигмы программирования:
Дан список чисел numbers = [1, 2, 3, 4, 5, 6]. Оставить только чётные, возвести их в квадрат и посчитать сумму.

Процедурный стиль
```python
numbers = [1, 2, 3, 4, 5, 6]

even_numbers = []
for n in numbers:
    if n % 2 == 0:
        even_numbers.append(n)

squared = []
for n in even_numbers:
    squared.append(n * n)

result = 0
for n in squared:
    result += n

print(result)  # 56
```

ООП-стиль
```python
class NumberProcessor:
    def __init__(self, numbers):
        self.numbers = numbers

    def filter_even(self):
        self.numbers = [n for n in self.numbers if n % 2 == 0]
        return self

    def square(self):
        self.numbers = [n * n for n in self.numbers]
        return self

    def sum(self):
        return sum(self.numbers)


numbers = [1, 2, 3, 4, 5, 6]
processor = NumberProcessor(numbers)
result = processor.filter_even().square().sum()
print(result)  # 56
```

Функциональный стиль
```python
from functools import reduce

numbers = [1, 2, 3, 4, 5, 6]

result = reduce(
    lambda acc, x: acc + x,
    map(
        lambda x: x * x,
        filter(lambda x: x % 2 == 0, numbers)
    ),
    0
)

print(result)  # 56
```

Асинхронный стиль
```python
import asyncio

numbers = [1, 2, 3, 4, 5, 6]


async def process_number(n: int) -> int | None:
    # типа ждём внешнего сервиса
    await asyncio.sleep(0.01)
    if n % 2 != 0:
        return None
    return n * n


async def main():
    tasks = [process_number(n) for n in numbers]
    results = await asyncio.gather(*tasks)

    squared_evens = [x for x in results if x is not None]
    result = sum(squared_evens)
    print(result)  # 56


asyncio.run(main())
```
Во всех четырёх примерах задача и результат абсолютно одинаковые, меняется только стиль/парадигма.
