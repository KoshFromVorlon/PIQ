Да ✅, в Python индекс списка может быть отрицательным.

---

## 📂 Как это работает

- **Положительные индексы** (`0, 1, 2, ...`) — отсчёт идёт **с начала списка**.
- **Отрицательные индексы** (`-1, -2, -3, ...`) — отсчёт идёт **с конца списка**.

---

## 🧩 Пример

```python
nums = [10, 20, 30, 40, 50]

print(nums[0])  # 10  (первый элемент)
print(nums[-1])  # 50  (последний элемент)
print(nums[-2])  # 40  (предпоследний элемент)
```

---

## 🎯 Итог

- **Да, индекс может быть отрицательным.**
- Это удобный способ обращаться к элементам списка с конца, не зная его длину.

---
