### 🔎 Лямбда‑функция в Python

**Лямбда‑функция** — это анонимная (безымянная) функция, которая создаётся с помощью ключевого слова `lambda`. Она
позволяет описывать небольшие функции прямо "на месте", без использования `def`.

---

## 📌 Синтаксис

lambda <аргументы>: <выражение>

- **аргументы** — список параметров (как у обычной функции).
- **выражение** — одно выражение, результат которого возвращается автоматически.

---

## 🎯 Назначение лямбда‑функций

Лямбда‑функции применяются там, где нужна **короткая функция на один раз**, без явного объявления имени. Основные
случаи:

- **Встроенные функции высшего порядка**
    - `map()`, `filter()`, `reduce()`, `sorted()` — удобно передавать лямбда как аргумент.
- **Одноразовые вычисления**
    - Когда нет смысла создавать отдельную именованную функцию.
- **Функциональное программирование**
    - Используются для компактного описания операций над данными.

---

## 🧩 Примеры

```python
# Удвоение числа
double = lambda x: x * 2
print(double(5))  # 10

# Использование в map()
nums = [1, 2, 3, 4]
squares = list(map(lambda n: n ** 2, nums))
print(squares)  # [1, 4, 9, 16]

# Сортировка по длине строки
words = ["python", "ai", "lambda"]
sorted_words = sorted(words, key=lambda w: len(w))
print(sorted_words)  # ['ai', 'lambda', 'python']
```

---

## ⚡ Отличия от обычных функций

| Обычная функция (`def`)             | Лямбда‑функция (`lambda`)         |
|-------------------------------------|-----------------------------------|
| Может содержать несколько выражений | Только одно выражение             |
| Имеет имя                           | Может быть анонимной              |
| Подходит для сложной логики         | Используется для простых операций |

---

👉 Иными словами, лямбда‑функция — это **короткий способ описать простую операцию прямо в коде**, без лишнего объявления.

