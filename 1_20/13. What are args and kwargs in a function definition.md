### 🔎 Что такое `*args` и `**kwargs` в Python

Они используются в определении функций для передачи **произвольного количества аргументов**.

---

## 📂 `*args`

- Позволяет функции принимать **любое количество позиционных аргументов**.
- Внутри функции `args` — это **кортеж**.

```python
def example(*args):
    print(args)


example(1, 2, 3)  # (1, 2, 3)
```

👉 Здесь функция получила три аргумента, но не было заранее известно, сколько их будет.

---

## 📂 `**kwargs`

- Позволяет функции принимать **любое количество именованных аргументов** (ключ=значение).
- Внутри функции `kwargs` — это **словарь**.

```python
def example(**kwargs):
    print(kwargs)


example(a=1, b=2, c=3)  # {'a': 1, 'b': 2, 'c': 3}
```

👉 Здесь функция получила три именованных аргумента.

---

## 🧩 Комбинированное использование

Можно использовать оба варианта одновременно:

```python
def example(*args, **kwargs):
    print("args:", args)
    print("kwargs:", kwargs)


example(1, 2, 3, x=10, y=20)
# args: (1, 2, 3)
# kwargs: {'x': 10, 'y': 20}
```

---

## 🎯 Итог

- **`*args`** → собирает позиционные аргументы в кортеж.
- **`**kwargs`** → собирает именованные аргументы в словарь.
- Вместе они делают функцию гибкой и универсальной.

---